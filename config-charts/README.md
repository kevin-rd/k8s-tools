# config-charts

Config chart examples and conventions.

These examples are not published by the `biz-charts` release workflow. They are
intended to be copied into business repositories when a service needs to ship
its own configuration template chart.

